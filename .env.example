# ====================================
# Golden Logistics - Environment Variables
# ====================================
# Copy this file to .env.local and fill in the values

# ====================================
# SUPABASE (Required)
# ====================================
# Get these from: https://supabase.com/dashboard/project/<your-project>/settings/api

# Public URL - safe to expose to client
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon/Public Key - safe to expose to client (Row Level Security handles access)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key - NEVER expose to client, server-side only
# Used for admin operations that bypass RLS
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ====================================
# SITE CONFIG (Optional)
# ====================================
# Base URL for canonical URLs, OG images, sitemaps
NEXT_PUBLIC_BASE_URL=https://your-domain.com

# ====================================
# SEED/MIGRATION (Optional)
# ====================================
# Direct database connection for migrations/seeds
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# Get this from: Supabase Dashboard > Settings > Database > Connection string
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres

# ====================================
# IMPORTANT SECURITY NOTES
# ====================================
# 1. NEVER commit .env.local to git (it's in .gitignore)
# 2. NEXT_PUBLIC_* vars are exposed to the browser - only use for public keys
# 3. SUPABASE_SERVICE_ROLE_KEY bypasses RLS - only use server-side
# 4. DATABASE_URL contains your DB password - never expose
#
# For production deployment (Vercel):
# - Add these as Environment Variables in project settings
# - NEXT_PUBLIC_* vars will be available client-side
# - Other vars are server-side only
